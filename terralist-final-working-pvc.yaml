apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "terralist.fullname" . }}
  labels:
    {{- include "terralist.labels" . | nindent 4 }}
spec:
  serviceName: {{ include "terralist.fullname" . }}
  replicas: 1
  selector:
    matchLabels:
      {{- include "terralist.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "terralist.selectorLabels" . | nindent 8 }}
    spec:
      enableServiceLinks: false
      initContainers:
      - name: init-permissions
        image: busybox:1.35
        command: ['sh', '-c']
        args:
          - |
            # Создаем директорию для данных
            mkdir -p /data
            # Устанавливаем права доступа для основного контейнера
            chown -R 1000:1000 /data
            chmod -R 755 /data
            echo "Permissions set for /data"
            ls -la /data/
      - name: restore-db
        image: busybox:1.35
        command: ['sh', '-c']
        args:
          - |
            # Восстанавливаем базу данных из persistent storage
            if [ -f /data/terralist.db ]; then
              echo "Restoring database from persistent storage..."
              cp /data/terralist.db /tmp/terralist.db
              echo "Database restored successfully"
            else
              echo "No existing database found, will create new one"
            fi
        volumeMounts:
        - name: data
          mountPath: /data
      containers:
      - name: terralist
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        # БЕЗ command, БЕЗ args, БЕЗ securityContext!
        ports:
        - name: http
          containerPort: 5758
          protocol: TCP
        env:
        - name: TERRALIST_DATABASE_BACKEND
          value: "sqlite"
        - name: TERRALIST_SQLITE_PATH
          value: "/tmp/terralist.db"
        - name: TERRALIST_SERVER_HOST
          value: "0.0.0.0"
        - name: TERRALIST_SERVER_PORT
          value: "5758"
        - name: TERRALIST_URL
          value: "https://terralist.caas.k-mkaas-dev-1.cloud.preprod.world"
        - name: TERRALIST_OAUTH_PROVIDER
          value: "oidc"
        - name: TERRALIST_OI_SCOPE
          value: "openid email profile"
        - name: TERRALIST_OI_CLIENT_ID
          value: {{ .Values.config.auth.oauth.clientId | quote }}
        - name: TERRALIST_OI_CLIENT_SECRET
          value: {{ .Values.config.auth.oauth.clientSecret | quote }}
        - name: TERRALIST_OI_AUTHORIZE_URL
          value: "{{ .Values.config.auth.oauth.issuerUrl }}/protocol/openid-connect/auth"
        - name: TERRALIST_OI_TOKEN_URL
          value: "{{ .Values.config.auth.oauth.issuerUrl }}/protocol/openid-connect/token"
        - name: TERRALIST_OI_USERINFO_URL
          value: "{{ .Values.config.auth.oauth.issuerUrl }}/protocol/openid-connect/userinfo"
        - name: TERRALIST_COOKIE_SECRET
          value: {{ .Values.config.auth.cookieSecret | quote }}
        - name: TERRALIST_TOKEN_SIGNING_SECRET
          value: {{ .Values.config.auth.tokenSigningSecret | quote }}
        - name: TERRALIST_ALLOW_ANONYMOUS_DOWNLOADS
          value: "true"
        volumeMounts:
        - name: data
          mountPath: /data
        lifecycle:
          preStop:
            exec:
              command:
              - /bin/sh
              - -c
              - |
                echo "Saving database before shutdown..."
                cp /tmp/terralist.db /data/terralist.db || true
                sync
                echo "Database saved"
        {{- if .Values.livenessProbe.enabled }}
        livenessProbe:
          tcpSocket:
            port: {{ .Values.livenessProbe.tcpSocket.port }}
          initialDelaySeconds: {{ .Values.livenessProbe.initialDelaySeconds }}
          timeoutSeconds: {{ .Values.livenessProbe.timeoutSeconds }}
          periodSeconds: {{ .Values.livenessProbe.periodSeconds }}
          successThreshold: {{ .Values.livenessProbe.successThreshold }}
          failureThreshold: {{ .Values.livenessProbe.failureThreshold }}
        {{- end }}
        {{- if .Values.readinessProbe.enabled }}
        readinessProbe:
          tcpSocket:
            port: {{ .Values.readinessProbe.tcpSocket.port }}
          initialDelaySeconds: {{ .Values.readinessProbe.initialDelaySeconds }}
          timeoutSeconds: {{ .Values.readinessProbe.timeoutSeconds }}
          periodSeconds: {{ .Values.readinessProbe.periodSeconds }}
          successThreshold: {{ .Values.readinessProbe.successThreshold }}
          failureThreshold: {{ .Values.readinessProbe.failureThreshold }}
        {{- end }}
        resources:
          {{- toYaml .Values.resources | nindent 10 }}
      - name: db-sync
        image: busybox:1.35
        command: ['sh', '-c']
        args:
          - |
            # Периодически сохраняем базу данных
            echo "Starting database sync sidecar..."
            while true; do
              sleep 30
              if [ -f /tmp/terralist.db ]; then
                cp /tmp/terralist.db /data/terralist.db
                echo "$(date): Database synced to persistent storage"
              fi
            done
        volumeMounts:
        - name: data
          mountPath: /data
  volumeClaimTemplates:
  - metadata:
      name: data
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: {{ .Values.persistence.size | default "1Gi" }}